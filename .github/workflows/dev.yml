name : list files

on: [push]

jobs: 
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Setup 
    - uses: actions/checkout@v2

    - name: test
      run: Hello World!

    - name: files
      run: ls -ra
